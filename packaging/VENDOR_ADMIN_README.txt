TileVision AI — Vendor Admin (DO NOT SEND TO CUSTOMERS)
=========================================================

This installer is for YOU (the vendor) only.

Use it on your Windows PC to:
  - Generate trial and full license keys
  - Manage customer registry and backups

Your private signing key is stored at:
  %USERPROFILE%\.tilevision_ai_vendor\vendor_private_key.pem

That file is NOT inside this installer. On first run, click
"Create New Key" OR copy your existing vendor_private_key.pem
into that folder before opening the admin app.

Customer installers (send these to clients):
  - TileVisionAI-Setup-*.exe          (Windows showrooms)
  - TileVisionAI-macOS-Intel-*.dmg    (Intel Mac)
  - TileVisionAI-macOS-AppleSilicon-*.dmg (M1/M2/M3/M4 Mac)

Never email or WhatsApp this admin installer to customers.

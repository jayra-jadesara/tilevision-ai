# Bundled DINOv2 weights for offline installs.
#
# Download once (requires internet, ~1 GB):
#   python scripts/download_dinov2_model.py
#
# This creates model_weights/dinov2-large/ which PyInstaller can bundle.
# The folder contents are gitignored — do not commit the weights themselves.
